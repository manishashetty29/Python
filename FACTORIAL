def fact(n):
    if n < 0:
        return "Factorial not defined for negative numbers"
    elif n <= 1:
        return 1
    else:
        return n * fact(n - 1)

n = int(input("Enter any number: "))
print(fact(n))
